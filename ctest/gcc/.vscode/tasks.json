{
    "version": "2.0.0",
    "tasks":[
        {
        "type": "shell",
            "label": "gcc",
            "command": "gcc.exe",
            "args": [
                 "-g",
                 "-o",
                 "${fileDirname}/${fileBasenameNoExtension}.exe",
                 "${file}"
            ],
            "problemMatcher":"$msCompile",
            "group": {
                "kind": "build",
                "isDefault": true
            }

        }
    ],
}